import { ThemedView } from "@/components/ThemedView";

export default function Orders() {
  return (
    <ThemedView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    ></ThemedView>
  );
}
