import { ThemedView } from "@/components/ThemedView";

export default function Camera() {
  return (
    <ThemedView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    ></ThemedView>
  );
}
